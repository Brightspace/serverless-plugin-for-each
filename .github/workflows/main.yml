name: Serverless Plugin For Each CI
on:
  push:
    branches:
      - master
  pull_request:
  workflow_dispatch:

jobs:
  library-ci:
    uses: Brightspace/space-github-actions/.github/workflows/library-ci.yml@v9
    secrets: inherit
